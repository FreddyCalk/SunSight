# Edge Function secrets. Generate distinct values per environment and never
# commit a populated .env file.
PHONE_HMAC_SECRET=replace-with-the-same-versioned-secret-provisioned-in-vault
DISPATCH_WORKER_SECRET=replace-with-at-least-32-random-bytes
